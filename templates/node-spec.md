### N{{id}} {{name}}

**节点目的**  
说明该节点为什么存在，以及它对最终任务结果的贡献。

**进入条件**  
用明确条件描述何时进入，不写“必要时”。

**输入 / 依赖状态**  
只列产品层需要存在的信息，不写代码对象。

**Agent 行为**  
说明模型或系统在该节点应该表现成什么样。

**产品规则与阈值**  
所有影响行为、成本、安全、质量、收敛、验收的值写死。

**输出 / 状态更新**  
说明节点完成后产生什么可用状态/结果。

**分支与下一节点**  
每个分支必须有条件和明确去向。

**异常与兜底**  
说明失败次数、重试、降级、Partial Success 或 Failure 出口。

**用户可见状态**  
没有用户可见变化时写“无”；不要留空。

**验收标准**  
必须可测试或可评测。
